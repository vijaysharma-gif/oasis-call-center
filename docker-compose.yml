services:

  backend:
    build:
      context: ./backend
    container_name: oasis-backend
    restart: unless-stopped
    env_file: ./backend/.env
    environment:
      NODE_ENV: production
      PORT: 3001
      LOG_DIR: /app/logs
    expose:
      - "3001"
    volumes:
      - ./logs:/app/logs
    networks:
      - oasis

  frontend:
    build:
      context: ./frontend
    container_name: oasis-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - oasis

networks:
  oasis:
    driver: bridge
